\documentclass[a4paper,oneside,11pt]{book}

% Package dasar
\usepackage[utf8]{inputenc}     % untuk karakter UTF-8
\usepackage[T1]{fontenc}        % encoding font
\usepackage{graphicx}
\usepackage{titling}
\usepackage{titlesec}
\usepackage[backend=biber,style=ieee]{biblatex}
\addbibresource{ref.bib}

% Package tambahan untuk layout dan formatting
\usepackage{geometry}           % untuk margin
\usepackage{setspace}           % untuk spasi baris
\usepackage{fancyhdr}           % untuk header/footer
\usepackage{hyperref}           % untuk link dan bookmark
\usepackage{listings}           % untuk code Python
\usepackage{xcolor}             % untuk warna
\usepackage{float}              % untuk posisi gambar
\usepackage{tocloft}            % Untuk membuat daftar isi menampilkan "Bab 1. Pendahuluan" instead of "1. Pendahuluan",
\usepackage{lipsum}             % untuk lorem ipsum
\usepackage{indentfirst}        % untuk indent paragraf pertama
\usepackage{enumitem}           % untuk kustomisasi penomoran list (A., B., dst)

% Pengaturan layout
\geometry{left=3cm, right=2cm, top=2cm, bottom=2cm}
\onehalfspacing  % spasi 1.5

% Penomoran section dengan huruf kapital (A, B, C, ...)
\renewcommand{\thesection}{\Alph{section}}
\renewcommand{\thesubsection}{\thesection.\arabic{subsection}}
\renewcommand{\thesubsubsection}{\thesubsection.\arabic{subsubsection}}

% Penomoran figure mengikuti section (misal, Gambar A.1, A.2, ...)
\renewcommand{\thefigure}{\thesection.\arabic{figure}}
\makeatletter
\@addtoreset{figure}{section}
\makeatother

% Format chapter
\titleformat{\chapter}[hang]
  {\normalfont\huge\bfseries\centering}{\chaptertitlename\ \thechapter.}{1em}{}

% Format code Python
% --- VS Code-like code style (dark) ---
\definecolor{vscodeBg}{HTML}{1E1E1E}
\definecolor{vscodeFg}{HTML}{D4D4D4}
\definecolor{vscodeKeyword}{HTML}{569CD6}
\definecolor{vscodeString}{HTML}{CE9178}
\definecolor{vscodeComment}{HTML}{6A9955}
\definecolor{vscodeNumber}{HTML}{858585}
\definecolor{vscodeRule}{HTML}{3C3C3C}

\lstdefinestyle{vscode-dark}{
  language=PHP,
  basicstyle=\ttfamily\small\color{vscodeFg},
  backgroundcolor=\color{vscodeBg},
  keywordstyle=\color{vscodeKeyword}\bfseries,
  commentstyle=\color{vscodeComment},
  stringstyle=\color{vscodeString},
  numbers=left,
  numberstyle=\tiny\color{vscodeNumber},
  frame=single,
  rulecolor=\color{vscodeRule},
  breaklines=true,
  showstringspaces=false,
  columns=fullflexible,
  keepspaces=true,
  tabsize=2,
  xleftmargin=2cm,
  xrightmargin=1cm,
  captionpos=b,
  morekeywords={Route,Controller,Request,Response,Blade,Model,Migration,Artisan,middleware,Auth,extends,section,yield,csrf,foreach,endif}
}

\lstset{style=vscode-dark}

% Pengaturan hyperref
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    filecolor=magenta,
    linktoc=all,
    urlcolor=blue,
    citecolor=red
}

% Info dokumen
\newcommand{\Pertemuan}{10}
\newcommand{\Materi}{Docker Compose}
\title{Laporan Praktikum \\ Sistem Administrasi dan Informasi Terdistribusi\\
Pertemuan \Pertemuan\\
\Materi}
\newcommand{\NamaMahasiswa}{Muhammad Fauzan Putra Trisuladana}
\newcommand{\NIMMahasiswa}{24/538699/SV/24524}
\newcommand{\KelasMahasiswa}{A1}
\newcommand{\DosenPengampu}{Dr. Eng. Ir. Ganjar Alfian, S.T., M. Eng.}

\author{\NamaMahasiswa\\\NIMMahasiswa}

\begin{document}

% Jika ingin daftar pustaka muncul meskipun belum ada entri di ref.bib,
% tambahkan entri BibTeX ke file ref.bib terlebih dulu.

% Title Page
\begin{titlingpage} 
\begin{center}

\vspace{4cm} 
\begin{Large} 
\textbf{\thetitle} \\
\end{Large}
\vspace{2cm}

\includegraphics[height=8cm]{resource/lambang ugm.png}\\ 
\vspace{2cm}

\textbf{Disusun oleh:}\\[0.8em]
\begin{minipage}{0.8\textwidth}
\raggedright
\begin{large}
\begin{tabular}{@{}l@{\hspace{1.5em}}c@{\hspace{1.5em}}p{\textwidth}@{}}
Nama & : & \NamaMahasiswa \\
NIM & : & \NIMMahasiswa \\
Kelas & : & \KelasMahasiswa \\
Dosen Pengampu & : & \DosenPengampu \\
\end{tabular}
\end{large}
\end{minipage}

\vspace{2cm}
\begin{Large}
\textbf{Program Studi D-IV Teknologi Rekayasa Perangkat Lunak} \\
\textbf{Departemen Teknik Elektro dan Informatika} \\
\textbf{Sekolah Vokasi}\\
\textbf{Universitas Gadjah Mada}\\
\textbf{Yogyakarta}\\
\textbf{2026}\\
\end{Large}

\end{center}
\end{titlingpage}

% Header/Footer
\pagestyle{fancy}
\fancyhf{}            % kosongkan header/footer default
\renewcommand{\headrulewidth}{0pt} % hilangkan garis header (atas)
\renewcommand{\footrulewidth}{0pt} % pastikan tidak ada garis footer
\fancyfoot[C]{\thepage} % taruh nomor halaman di footer tengah

% Pastikan halaman dengan style 'plain' (mis. ToC/List) juga tanpa garis atas
\fancypagestyle{plain}{
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0pt}
  \fancyfoot[C]{\thepage}
}


% --- Styling Judul Daftar Isi, Gambar, dan Kode ---
\renewcommand{\cfttoctitlefont}{\hfill\normalfont\LARGE\bfseries}
\renewcommand{\cftaftertoctitle}{\hfill}
\renewcommand{\cftbeforetoctitleskip}{1em}
\renewcommand{\cftaftertoctitleskip}{1.5em}

% --- Format Bab di Daftar Isi ---
\renewcommand{\cftchappresnum}{Bab~}
\renewcommand{\cftchapaftersnum}{.}
\renewcommand{\cftchapnumwidth}{3.5em}
\renewcommand{\cftchapfont}{\bfseries}
\renewcommand{\cftchappagefont}{\bfseries}

% --- Spasi dan Estetika ---
\setlength{\cftbeforechapskip}{0.8em} % jarak antar bab
\setlength{\cftbeforesecskip}{0.2em}  % jarak antar subbab
\renewcommand{\cftsecfont}{\normalfont}
\renewcommand{\cftsubsecfont}{\normalfont}

% --- Judul Lokal ---
\renewcommand{\contentsname}{\centering Daftar Isi}
\renewcommand{\listfigurename}{\centering Daftar Gambar}
\renewcommand{\lstlistlistingname}{\centering Daftar Kode}

% --- Gaya Daftar Gambar dan Daftar Kode ---
\renewcommand{\cftloftitlefont}{\hfill\normalfont\LARGE\bfseries}
\renewcommand{\cftafterloftitle}{\hfill}

\tableofcontents

\newpage
% Daftar Gambar
\listoffigures

\newpage
% Daftar Listing
\lstlistoflistings

% --- Halaman Laporan Pertemuan (seperti contoh gambar) ---
% Tidak pakai \chapter agar bisa menyatu dengan halaman sebelumnya.
\newpage
\begin{center}
\textbf{\LARGE Pertemuan \Pertemuan\\
{[{\Materi}]}}
\end{center}

\section{Tujuan Praktikum}
\begin{enumerate}
  \item Memahami perbedaan antara Dockerfile (membangun image) dan Docker Compose (menjalankan container)
  \item Menstranslasikan perintah CLI docker run yang panjang menjadi file konfigurasi docker-compose.yml
  \item Mengimplementasikan fitur Volumes untuk Hot-Reloading (perubahan kode langsung terlihat tanpa rebuild).
  \item Mengelola environment variables menggunakan .env yang terintegrasi dengan Docker Compose
\end{enumerate}

\section{Dasar Teori}
\subsection{MySQL}

\par MySQL adalah sistem manajemen basis data relasional (RDBMS) yang menggunakan Structured Query Language (SQL) untuk melakukan definisi skema, manipulasi data, dan query. MySQL menerapkan arsitektur client--server, di mana proses server (\texttt{mysqld}) menyimpan serta mengelola data, sedangkan klien (misalnya \texttt{mysql}) digunakan untuk terhubung dan mengeksekusi perintah SQL. Dalam praktik pengembangan aplikasi, MySQL banyak dipakai karena performanya baik, dukungan ekosistem luas, serta ketersediaan dokumentasi yang lengkap \cite{mysql-refman}.

\par Pada sisi administrasi sistem, konfigurasi MySQL umumnya dilakukan melalui berkas konfigurasi (misalnya \texttt{mysqld.cnf} di Linux atau \texttt{my.ini} di Windows) untuk mengatur parameter layanan seperti port, direktori data, serta kebijakan akses jaringan melalui \texttt{bind-address}. Selain itu, MySQL memiliki mekanisme autentikasi dan hak akses (privileges) untuk memastikan hanya user tertentu yang dapat mengakses database sesuai perannya \cite{mysql-refman}.

\subsection{REST API}
\par REST API atau \textit{Representational State Transfer Application Programming Interface} adalah antarmuka pemrograman aplikasi yang mengikuti prinsip-prinsip arsitektur REST. REST sendiri diperkenalkan oleh Roy Fielding sebagai gaya arsitektur untuk sistem hipermedia terdistribusi, dan kemudian menjadi salah satu pendekatan yang paling banyak digunakan dalam pengembangan layanan web modern \cite{ibm-rest-api,restfulapi-rest}. Pada pendekatan ini, data dan fungsi dipandang sebagai \textit{resource} yang diidentifikasi melalui URI, lalu diakses atau dimanipulasi menggunakan antarmuka yang seragam. Karena sifatnya ringan, fleksibel, dan mudah diintegrasikan dengan berbagai bahasa pemrograman serta format data, REST API banyak digunakan untuk pertukaran data antara frontend, backend, layanan web, maupun database \cite{ibm-rest-api}.

\par Sebuah REST API yang baik dibangun berdasarkan sejumlah kendala arsitektural utama, yaitu \textit{uniform interface}, \textit{client-server}, \textit{stateless}, \textit{cacheable}, \textit{layered system}, dan \textit{code on demand} sebagai sifat opsional \cite{ibm-rest-api,restfulapi-rest}. Prinsip \textit{client-server} menekankan pemisahan tanggung jawab antara sisi klien dan sisi server, sehingga antarmuka pengguna dapat berkembang secara independen dari logika dan penyimpanan data di server. Sementara itu, sifat \textit{stateless} mengharuskan setiap request membawa seluruh informasi yang dibutuhkan untuk diproses, tanpa bergantung pada sesi yang disimpan di server. Pendekatan ini membuat REST API lebih sederhana, mudah diskalakan, dan lebih cocok digunakan pada arsitektur sistem terdistribusi \cite{ibm-rest-api,restfulapi-rest}.

\par Dalam implementasinya, REST API sangat sering berjalan di atas protokol HTTP, walaupun REST dan HTTP bukanlah hal yang sama \cite{restfulapi-rest}. Pada praktik umum, method HTTP seperti \texttt{GET}, \texttt{POST}, \texttt{PUT}, dan \texttt{DELETE} digunakan untuk mendukung operasi \textit{create}, \textit{read}, \textit{update}, dan \textit{delete} pada resource. Data hasil pertukaran biasanya direpresentasikan dalam format JSON karena mudah dibaca oleh manusia maupun mesin, serta bersifat independen terhadap bahasa pemrograman \cite{ibm-rest-api}. Selain itu, request dan response juga memanfaatkan header, parameter, serta status code HTTP untuk menyampaikan metadata, otorisasi, keberhasilan proses, maupun informasi kesalahan.

\subsection{API Authentication}

\par \textit{API authentication} adalah proses untuk memverifikasi identitas pihak yang melakukan request ke sebuah API, sehingga server dapat memastikan bahwa request benar-benar berasal dari pengguna atau client yang sah. Pada praktiknya, autentikasi pada REST API umumnya tidak menggunakan sesi yang disimpan di server (\textit{stateless}), melainkan menggunakan kredensial yang dikirim pada setiap request melalui header HTTP.

\par Salah satu pendekatan yang banyak digunakan adalah autentikasi berbasis token, di mana server mengeluarkan token setelah proses login berhasil, lalu token tersebut digunakan kembali pada request berikutnya. Token akan menjadi bukti autentikasi, sedangkan aturan akses (misalnya berdasarkan role) dapat diterapkan sebagai bagian dari proses otorisasi setelah token diverifikasi.

\par JSON Web Token (JWT) merupakan standar token berbasis JSON yang bersifat ringkas dan aman untuk ditransmisikan, serta digunakan untuk merepresentasikan klaim (\textit{claims}) antar pihak. JWT umumnya memiliki tiga bagian utama, yaitu \textit{header}, \textit{payload}, dan \textit{signature} (format \texttt{header.payload.signature}). Signature digunakan untuk memastikan integritas token, sehingga payload tidak dapat diubah tanpa mengetahui kunci penandatanganan \cite{rfc7519-jwt}.

\par Dalam konteks HTTP, JWT sering dikirim sebagai \textit{bearer token} menggunakan header dengan nama \texttt{Authorization} dengan skema \texttt{Bearer}. Skema bearer token ini didefinisikan pada RFC 6750 dan menjadi pola umum pada layanan API modern karena sederhana dan sesuai untuk request yang \textit{stateless} \cite{rfc6750-bearer}. Dengan mekanisme ini, server dapat memverifikasi signature JWT, membaca klaim yang diperlukan (misalnya \texttt{id} dan \texttt{role}), lalu menentukan apakah request berhak mengakses endpoint tertentu. JWT juga biasanya memiliki masa berlaku (misalnya klaim \texttt{exp}) untuk membatasi risiko penyalahgunaan token \cite{rfc7519-jwt}.

\subsection{Hoppscotch API Documentation Tool}

\par Hoppscotch adalah \textit{API development ecosystem} berbasis web yang dapat digunakan untuk menguji dan mendokumentasikan endpoint API. Tool ini memudahkan pengguna dalam menyusun request (method, URL, header, dan body), melihat response (status code, header, dan payload JSON), serta melakukan iterasi pengujian dengan cepat \cite{hoppscotch-docs}.

\par Dalam pengujian REST API, Hoppscotch menyediakan fitur \textit{collections} untuk mengelompokkan request, \textit{environment variables} untuk menyimpan nilai yang sering dipakai (contoh: \textit{base URL} dan token), serta dukungan skrip (\textit{pre/post-request}) untuk otomatisasi sederhana seperti menyimpan token dari response login. Selain itu, request yang sudah disusun dapat diekspor sebagai file JSON sehingga pengujian dan dokumentasi dapat direplikasi secara konsisten \cite{hoppscotch-docs,hoppscotch-github}.

\subsection{Docker}

\par Docker adalah platform \textit{containerization} yang digunakan untuk mengembangkan, mengirim, dan menjalankan aplikasi di dalam lingkungan terisolasi yang disebut \textit{container}. Berbeda dengan pendekatan virtualisasi tradisional yang menggunakan \textit{virtual machine} (VM), Docker memanfaatkan teknologi virtualisasi tingkat sistem operasi sehingga container dapat berjalan lebih ringan, cepat, dan efisien dalam penggunaan sumber daya \cite{docker-docs}.

\par Pada arsitekturnya, Docker terdiri dari beberapa komponen utama, yaitu \textit{Docker Engine} sebagai layanan utama yang mengelola container, \textit{Docker Image} sebagai template read-only untuk membuat container, serta \textit{Docker Container} sebagai instansi yang berjalan dari sebuah image. Image biasanya dibangun menggunakan berkas \texttt{Dockerfile} yang berisi instruksi untuk menyusun lingkungan aplikasi, seperti sistem operasi dasar, dependensi, serta konfigurasi yang diperlukan \cite{docker-docs}.

\par Salah satu keunggulan utama Docker adalah portabilitas, di mana aplikasi yang telah dikemas dalam container dapat dijalankan secara konsisten di berbagai lingkungan, baik pada komputer lokal, server, maupun layanan cloud tanpa perlu konfigurasi ulang yang kompleks. Hal ini dimungkinkan karena seluruh dependensi aplikasi telah dibungkus dalam container tersebut \cite{docker-docs}.

\par Selain itu, Docker mendukung konsep \textit{image layering} dan \textit{caching} yang membuat proses build menjadi lebih efisien, serta memudahkan distribusi melalui \textit{container registry} seperti Docker Hub. Dalam praktik pengembangan modern, Docker sering digunakan bersama dengan alat orkestrasi seperti Kubernetes untuk mengelola deployment aplikasi dalam skala besar \cite{docker-docs}.

\subsection{Docker Compose}

\par Docker Compose adalah tool untuk mendefinisikan dan menjalankan aplikasi multi-container berbasis Docker. Dengan Docker Compose, konfigurasi beberapa container (misalnya backend API, database, dan tool pendukung) dituliskan dalam sebuah berkas YAML seperti defaultnya, \texttt{docker-compose.yml}, lalu seluruh stack dapat dijalankan atau dihentikan melalui satu perintah, contohnya \texttt{docker compose up} dan \texttt{docker compose down} \cite{docker-compose-docs}.

\par Berkas \texttt{docker-compose.yml} membantu menerjemahkan perintah \texttt{docker run} yang panjang menjadi deklarasi konfigurasi yang lebih terstruktur. Pada Compose, setiap container direpresentasikan sebagai \textit{service} dan dapat diberi pengaturan seperti \texttt{image} atau \texttt{build} (sumber image), \texttt{ports} (publikasi port), \texttt{volumes} (mount direktori/volume), \texttt{environment} (variabel lingkungan), serta \texttt{depends\_on} untuk mengatur ketergantungan antar service \cite{docker-compose-file}.

\par Docker Compose juga mendukung manajemen konfigurasi melalui variabel environment (misalnya memanfaatkan berkas \texttt{.env} dan opsi \texttt{env\_file}) sehingga parameter untuk terhubung ke layanan lain seperti database dapat diubah tanpa memodifikasi kode aplikasi \cite{docker-compose-docs}. Selain itu, Compose menyediakan perintah untuk observabilitas dan siklus hidup service seperti \texttt{docker compose ps}, \texttt{docker compose logs}, \texttt{docker compose restart}, serta kemampuan rebuild image melalui \texttt{docker compose build} ketika ada perubahan pada \texttt{Dockerfile} \cite{docker-compose-docs}.

\par Secara standar, format dan perilaku Compose mengacu pada \textit{Compose Specification} sehingga definisi service, network, dan volume dapat dipahami secara konsisten oleh berbagai implementasi yang mengikuti spesifikasi tersebut \cite{compose-spec}. Dalam praktik pengembangan, Docker Compose banyak digunakan untuk menyiapkan environment yang reproducible untuk development dan testing, serta memudahkan orkestrasi service yang saling terhubung dalam satu jaringan Docker \cite{docker-docs,docker-compose-docs}.

%contoh gambar dan kode
% \begin{figure}[H]
%     \centering
%     \includegraphics[width=0.5\textwidth]{resource/contoh-gambar.png}
%     \caption{Contoh Gambar}
%     \label{fig:contoh-gambar}
% \end{figure}

% \begin{lstlisting}[language=php, caption={Contoh Kode Python}, label={lst:contoh-kode}]
% def hello_world():
%     print("Hello, World!")
% \end{lstlisting}

\section{Hasil dan Pembahasan}

\par Pada pertemuan ini saya melakukan implementasi orkestrasi REST API berbasis PHP (JWT) menggunakan Docker Compose pada server VPS. Kode awal yang digunakan berasal dari praktikum sebelumnya dan pada pertemuan ini dikembangkan menjadi versi terbaru yang dijalankan menggunakan Docker Compose. Alur praktikum mengikuti urutan pada modul yang ada dibawah ini, dengan beberapa penyesuaian yang saya lakukan untuk memastikan integrasi antara service API dan database berjalan dengan baik.

\par Salah satu penyesuaian yang saya lakukan sebelum mengikuti langkah pada modul adalah mengubah konfigurasi koneksi database dari model \textit{hardcode} (kode lama) menjadi membaca variabel environment dari file \texttt{.env}. Perubahan ini membuat aplikasi lebih fleksibel karena nilai host, port, nama database, user, dan password dapat diubah tanpa memodifikasi kode PHP.

\par Secara ringkas, langkah yang saya lakukan adalah sebagai berikut:
\begin{enumerate}
  \item Mengubah konfigurasi koneksi database agar membaca variabel environment dari file \texttt{.env}.
  \item Membuat file \texttt{.env} untuk menyimpan parameter koneksi database.
  \item Menyiapkan struktur project versi terbaru (Docker Compose).
  \item Menghentikan dan menghapus container PHP-JWT lama dari pertemuan sebelumnya untuk menghindari konflik.
  \item Membuat file \texttt{docker-compose.yml} untuk menjalankan service \texttt{be-jwt}.
  \item Menjalankan \texttt{docker compose up -d} dan memverifikasi status service.
  \item Melakukan rebuild dan restart service ketika ada perubahan konfigurasi.
  \item Menambahkan service \texttt{database\_mysql} (MySQL) ke dalam \texttt{docker-compose.yml}.
  \item Menambahkan persistent volume \texttt{db\_data} dan melakukan mount SQL init \texttt{php\_jwt\_db.sql}.
  \item Menambahkan konfigurasi \texttt{depends\_on} agar urutan start container lebih terkontrol.
  \item Mengubah nilai \texttt{DB\_HOST} pada \texttt{.env} menjadi nama service \texttt{database\_mysql}.
  \item Menjalankan ulang Docker Compose hingga service API dan database aktif.
  \item Menguji endpoint \texttt{Cek}, \texttt{Register}, \texttt{Login}, dan \texttt{Get All Users} menggunakan Hoppscotch.
  \item Menguji \textit{hot reload} dengan menambahkan route \texttt{/docker} tanpa rebuild image.
  \item Mengerjakan Tugas \& Analisis: menambahkan service phpMyAdmin dan melakukan CRUD dari phpMyAdmin itu.
\end{enumerate}

\subsection{Migrasi Konfigurasi Database ke \texttt{.env}}
\par Pada kode lama, konfigurasi database sebelumnya masih menggunakan pendekatan \textit{hardcode}. Namun, pada pertemuan ini modul menyarankan penggunaan \texttt{.env} agar konfigurasi dapat diubah dengan cepat, terlebih saat database dijalankan sebagai service terpisah di jaringan Docker Compose code dirubah dari code \ref{lst:p10-old-db-config}.

\lstinputlisting[caption={Cuplikan konfigurasi database lama (hardcode)}, label={lst:p10-old-db-config}]{resource/jwt docker/config/database.php}

\par karena kode seperti itu \ref{lst:p10-old-db-config} susah di \textit{maintenance} dan setiap perubahan memerlukan rebuild image. Oleh karena itu, saya mengubah file \texttt{config/database.php} agar mengambil nilai host, port, nama database, username, dan password menggunakan fungsi \texttt{getenv()}. Proses perubahan ditunjukkan pada Gambar \ref{fig:p10-01-db-to-env}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/1 - mengganti bagian koneksi database ke env.png}
  \caption{Mengubah konfigurasi koneksi database agar membaca variabel environment}
  \label{fig:p10-01-db-to-env}
\end{figure}

\par Setelah itu, saya menambahkan file \texttt{.env} di root project untuk menyimpan parameter koneksi database (Gambar \ref{fig:p10-02-env-file}). Pada tahap awal, nilai \texttt{DB\_HOST} masih diarahkan ke IP VPS karena service database belum ditambahkan ke Docker Compose. Perubahan \texttt{DB\_HOST} menjadi nama service \texttt{database\_mysql} dibahas pada Gambar \ref{fig:p10-12-db-host-service}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/2 - file env nya.png}
  \caption{Membuat file \texttt{.env} untuk menyimpan parameter koneksi database}
  \label{fig:p10-02-env-file}
\end{figure}

\par Cuplikan perubahan pada file \texttt{config/database.php} dan isi \texttt{.env} dirangkum pada Kode \ref{lst:p10-db-conn-env} dan Kode \ref{lst:p10-env-initial}.

\begin{lstlisting}[caption={Cuplikan konfigurasi database berbasis environment pada \texttt{config/database.php}}, label={lst:p10-db-conn-env}]
public function __construct() {
    $this->db_host = getenv('DB_HOST');
    $this->db_name = getenv('DB_NAME');
    $this->db_user = getenv('DB_USER');
    $this->db_password = getenv('DB_PASSWORD');
    $this->db_port = getenv('DB_PORT');
}

$this->connection = new PDO(
    "mysql:host=" . $this->db_host . ";port=" . $this->db_port . ";dbname=" . $this->db_name,
    $this->db_user,
    $this->db_password
);
\end{lstlisting}

\begin{lstlisting}[caption={Contoh isi \texttt{.env} untuk parameter koneksi database (tahap awal)}, label={lst:p10-env-initial}]
DB_HOST=10.33.35.96
DB_PORT=3306
DB_NAME=sait_db
DB_USER=fauzan
DB_PASSWORD=fauzan123
\end{lstlisting}

\par Dengan pendekatan ini, konfigurasi koneksi database dapat diganti cukup melalui \texttt{.env} tanpa memodifikasi kode PHP, sehingga lebih sesuai untuk orkestrasi multi-service dengan Docker Compose.

\subsection{Struktur Project Versi Docker Compose}
\par Setelah penyesuaian koneksi database, struktur project versi terbaru sudah memiliki file \texttt{docker-compose.yml} dan \texttt{.env} pada root project. Struktur folder yang digunakan pada praktikum ini ditunjukkan pada Gambar \ref{fig:p10-03-structure-now}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/3 - struktur file sekarang.png}
  \caption{Struktur folder project versi terbaru (Docker Compose)}
  \label{fig:p10-03-structure-now}
\end{figure}

\subsection{Menghentikan dan Menghapus Container PHP-JWT Lama}
\par Sebelum menjalankan Docker Compose, saya menghentikan dan menghapus container lama dari pertemuan sebelumnya untuk menghindari konflik port dan mempermudah troubleshooting. Proses penghentian dan penghapusan container \texttt{jwt-app} ditunjukkan pada Gambar \ref{fig:p10-04-stop-remove-old}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/4 - Menghentikan dan menghapus kontainer php-jwt lama.png}
  \caption{Menghentikan dan menghapus container PHP-JWT lama dari pertemuan sebelumnya}
  \label{fig:p10-04-stop-remove-old}
\end{figure}

\par Perintah yang digunakan untuk menghentikan dan menghapus container dirangkum pada Kode \ref{lst:p10-stop-remove-old}.

\begin{lstlisting}[caption={Perintah menghentikan dan menghapus container lama}, label={lst:p10-stop-remove-old}]
docker ps -a
docker stop jwt-app
docker rm jwt-app
docker ps -a
\end{lstlisting}

\par Dengan langkah tersebut, service lama sudah tidak berjalan lagi, sehingga port yang dibutuhkan pada praktikum ini dapat digunakan tanpa tabrakan. Hal ini dibuktikan dengan pengecekan status container yang sudah tidak ada lagi (Gambar \ref{fig:p10-04-stop-remove-old} bagian bawah sendiri).

\subsection{Membuat File \texttt{docker-compose.yml}}
\par Docker Compose digunakan untuk menerjemahkan proses menjalankan container (yang sebelumnya dilakukan dengan perintah \texttt{docker run}) menjadi sebuah file konfigurasi yaitu file \texttt{docker-compose.yml} di root project. Pada tahap awal, saya membuat service \texttt{be-jwt} untuk menjalankan REST API PHP (JWT), serta mendefinisikan network \texttt{psait}. Proses penulisan file \texttt{docker-compose.yml} ditunjukkan pada Gambar \ref{fig:p10-05-compose-basic}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/5 - menambahkan docker compose.png}
  \caption{Membuat \texttt{docker-compose.yml} dan mendefinisikan service \texttt{be-jwt}}
  \label{fig:p10-05-compose-basic}
\end{figure}

\par Cuplikan konfigurasi \texttt{docker-compose.yml} pada tahap awal dirangkum pada Kode \ref{lst:p10-compose-basic} di bawah ini.

\begin{lstlisting}[caption={Cuplikan \texttt{docker-compose.yml} (tahap awal)}, label={lst:p10-compose-basic}]
services:
  be-jwt:
    container_name: php-jwt-zan
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/var/www/html
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - psait

networks:
  psait:
    driver: bridge
\end{lstlisting}

\par Pada konfigurasi di atas, service \texttt{be-jwt} menggunakan instruksi \texttt{build: .} sehingga Docker Compose akan membangun image berdasarkan \texttt{Dockerfile} yang sudah dibuat pada pertemuan sebelumnya. Port \texttt{8000:8000} digunakan agar aplikasi dapat diakses dari luar VPS, sedangkan \texttt{volumes} dipakai untuk melakukan mounting folder project ke dalam container \texttt{/var/www/html} sehingga perubahan kode dapat langsung di-reload (hot reload).

\subsection{Menjalankan Docker Compose Up}
\par Setelah file \texttt{docker-compose.yml} dibuat, saya menjalankan service menggunakan perintah \texttt{docker compose up -d}. Hasil eksekusi perintah tersebut ditunjukkan pada Gambar \ref{fig:p10-06-compose-up}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/6 - Menjalankan Docker Compose Up.png}
  \caption{Menjalankan service awal menggunakan \texttt{docker compose up -d}}
  \label{fig:p10-06-compose-up}
\end{figure}

\begin{lstlisting}[caption={Perintah menjalankan Docker Compose}, label={lst:p10-compose-up}]
docker compose up -d
\end{lstlisting}

\par Dengan perintah tersebut, Docker Compose akan membangun image (jika belum ada) dan menjalankan container sesuai definisi pada \texttt{docker-compose.yml}.

\subsection{Mengecek Status Docker Compose}
\par Untuk memastikan service sudah berjalan, saya mengecek status menggunakan perintah \texttt{docker compose ps -a}. Hasil pengecekan status ditunjukkan pada Gambar \ref{fig:p10-07-compose-ps}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/7 - docker compose ps -a di root yang ada docker compose yml.png}
  \caption{Mengecek status service menggunakan \texttt{docker compose ps -a}}
  \label{fig:p10-07-compose-ps}
\end{figure}

\begin{lstlisting}[caption={Perintah mengecek status Docker Compose}, label={lst:p10-compose-ps}]
docker compose ps -a
\end{lstlisting}

\par Pengecekan status ini penting untuk memastikan port mapping aktif, container dalam status \textit{running}, dan tidak ada error saat startup.

\subsection{Rebuild dan Menjalankan Ulang Service \texttt{be-jwt}}
\par Ketika ada perubahan pada konfigurasi build atau file \texttt{Dockerfile}, Docker Compose menyediakan perintah \texttt{docker compose build} untuk membangun ulang image. Setelah rebuild, service dapat dijalankan ulang menggunakan \texttt{docker compose restart}. Proses rebuild dan restart service \texttt{be-jwt} ditunjukkan pada Gambar \ref{fig:p10-08-compose-rebuild}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/8 - ujicoba Rebuild dan Menjalankan Ulang.png}
  \caption{Rebuild image dan restart service \texttt{be-jwt}}
  \label{fig:p10-08-compose-rebuild}
\end{figure}

\begin{lstlisting}[caption={Perintah rebuild dan restart service \texttt{be-jwt}}, label={lst:p10-compose-rebuild}]
docker compose build be-jwt
docker compose restart be-jwt
\end{lstlisting}

\par Pada kasus hot reload (perubahan kode PHP biasa), rebuild image umumnya tidak diperlukan karena file sudah di-mount melalui \texttt{volumes}. Namun, rebuild tetap diperlukan jika perubahan terkait dependensi atau konfigurasi image.

\subsection{Menambahkan Service \texttt{database\_mysql} (MySQL)}
\par Setelah service API berhasil dijalankan, tahap berikutnya sesuai modul adalah menambahkan service database di Docker Compose. Pada praktikum ini saya menambahkan service \texttt{database\_mysql} menggunakan image \texttt{mysql:8.0}. Penambahan service database ditunjukkan pada Gambar \ref{fig:p10-09-add-mysql}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/9 -  menambahkan database_mysql.png}
  \caption{Menambahkan service \texttt{database\_mysql} (MySQL) pada \texttt{docker-compose.yml}}
  \label{fig:p10-09-add-mysql}
\end{figure}

\par Pada service ini, saya menggunakan variabel dari \texttt{.env} untuk mengisi \texttt{MYSQL\_DATABASE}, \texttt{MYSQL\_USER}, dan \texttt{MYSQL\_PASSWORD}. Selain itu, file \texttt{php\_jwt\_db.sql} dimount ke direktori init MySQL agar database dapat dibuat otomatis saat container pertama kali dijalankan.

\subsection{Penambahan Persistent Volume MySQL}
\par Karena data pada container bersifat tidak persisten jika container dihapus, modul menyarankan penggunaan persistent volume agar data MySQL tetap tersimpan. Pada praktikum ini saya menambahkan volume bernama \texttt{db\_data} (Gambar \ref{fig:p10-10-volume}).

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/10 - Penambahan Persistent Volume.png}
  \caption{Menambahkan persistent volume \texttt{db\_data} untuk MySQL}
  \label{fig:p10-10-volume}
\end{figure}

\par Volume ini digunakan untuk mounting direktori data MySQL ke \texttt{/var/lib/mysql}, sehingga ketika container database restart, data tidak hilang.

\subsection{Menambahkan Ketergantungan Service (\texttt{depends\_on})}
\par Agar service API tidak berjalan sebelum database siap, saya menambahkan konfigurasi \texttt{depends\_on} pada service \texttt{be-jwt}. Penambahan ketergantungan service ditunjukkan pada Gambar \ref{fig:p10-11-depends-on}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/11 -  tambahkan depends on .png}
  \caption{Menambahkan \texttt{depends\_on} pada service \texttt{be-jwt}}
  \label{fig:p10-11-depends-on}
\end{figure}

\par Dengan \texttt{depends\_on}, Docker Compose akan menjalankan container database lebih dahulu, lalu menjalankan service API setelahnya. Walaupun ini belum menjamin database sudah \textit{ready}, konfigurasi ini membantu mengurangi kemungkinan error saat startup.

\subsection{Mengubah \texttt{DB\_HOST} menjadi Nama Service MySQL}
\par Setelah database dijalankan di Docker Compose, host database sebaiknya tidak lagi menggunakan IP VPS, melainkan menggunakan nama service \texttt{database\_mysql}. Hal ini karena Docker menyediakan DNS internal yang dapat menerjemahkan nama service menjadi alamat container dalam network Compose. Perubahan nilai \texttt{DB\_HOST} pada \texttt{.env} ditunjukkan pada Gambar \ref{fig:p10-12-db-host-service}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/12 - mengganti env db host ke database_mysql.png}
  \caption{Mengubah \texttt{DB\_HOST} ke \texttt{database\_mysql} agar koneksi DB menggunakan jaringan Docker Compose}
  \label{fig:p10-12-db-host-service}
\end{figure}

\par Setelah perubahan ini, koneksi dari aplikasi PHP di container \texttt{be-jwt} akan diarahkan ke container MySQL dalam network \texttt{psait}, bukan ke MySQL yang berjalan di host VPS.

\subsection{Menjalankan Ulang Docker Compose untuk Service Database}
\par Setelah seluruh konfigurasi service database ditambahkan, saya menjalankan kembali Docker Compose agar service \texttt{database\_mysql} dibuat dan dijalankan. Proses menjalankan ulang Docker Compose ditunjukkan pada Gambar \ref{fig:p10-13-compose-up-db}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/13 - Jalankan docker compose up untuk membuat service datbase.png}
  \caption{Menjalankan \texttt{docker compose up -d} setelah menambahkan service database}
  \label{fig:p10-13-compose-up-db}
\end{figure}

\par Pada tahap ini Docker akan menarik image \texttt{mysql:8.0}, membuat volume \texttt{db\_data}, serta menjalankan container database. Setelah semua service aktif, pengujian API dapat dilakukan.

\subsection{Pengujian Endpoint API Menggunakan Hoppscotch}
\par Setelah service API dan database aktif, saya melakukan pengujian endpoint menggunakan Hoppscotch. Untuk mempermudah, saya membuat environment variable \texttt{link} sebagai base URL (misalnya \texttt{http://10.33.35.96:8000}) dan variable \texttt{jwt} untuk menyimpan token hasil login yang saya masukkan ke env dari script (ada di 2 pertemuan sebelumnya). Pengaturan environment ditunjukkan pada Gambar \ref{fig:p10-14-hoppscotch-env}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/14 - untuk uji cek env di hoppscotch.png}
  \caption{Pengaturan environment Hoppscotch (base URL dan token JWT)}
  \label{fig:p10-14-hoppscotch-env}
\end{figure}

\par Setelah environment disiapkan, endpoint \texttt{Cek} diuji terlebih dahulu dan berhasil mengembalikan response JSON sederhana (Gambar \ref{fig:p10-15-cek}).

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/15 - pengujian route cek (root).png}
  \caption{Pengujian endpoint \texttt{Cek} menggunakan Hoppscotch}
  \label{fig:p10-15-cek}
\end{figure}

\par Berikutnya, endpoint \texttt{Register} diuji untuk memastikan data user dapat disimpan ke database (Gambar \ref{fig:p10-16-register}). Setelah register berhasil, endpoint \texttt{Login} diuji untuk memperoleh token JWT (Gambar \ref{fig:p10-17-login}). Token JWT tersebut digunakan sebagai bearer token untuk mengakses endpoint \texttt{Get All Users} (Gambar \ref{fig:p10-18-users}).

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/16 - register.png}
  \caption{Pengujian endpoint \texttt{Register} menggunakan Hoppscotch}
  \label{fig:p10-16-register}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/17 - login.png}
  \caption{Pengujian endpoint \texttt{Login} dan mendapatkan token JWT}
  \label{fig:p10-17-login}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/18 - get all users.png}
  \caption{Pengujian endpoint \texttt{Get All Users} menggunakan bearer token}
  \label{fig:p10-18-users}
\end{figure}

\par Hasil pengujian tersebut menunjukkan bahwa API-API itu semua berhasil terhubung dengan database MySQL di Docker Compose, proses register dan login berjalan dengan benar, dan endpoint yang memerlukan autentikasi (Get All Users) dapat diakses menggunakan token JWT.

\subsection{Uji \textit{Hot Reload} Menggunakan Volume Binding}
\par Salah satu tujuan penggunaan Docker Compose adalah memanfaatkan \textit{hot reload} melalui fitur volume binding. Karena folder project dimount ke container, perubahan kode di host dapat langsung tercermin di container tanpa rebuild image lagi.

\par Untuk membuktikan hot reload, saya menambahkan route \texttt{/docker} pada router \texttt{index.php} yang mengembalikan JSON \texttt{"Docker is working"}. Proses penambahan route ditunjukkan pada Gambar \ref{fig:p10-19-add-route-docker}, sedangkan hasil pengujiannya pada Hoppscotch ditunjukkan pada Gambar \ref{fig:p10-20-cek-docker}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/19 - menambahkan untuk menguji hot reload jadi ketika ini di save perubahan langsung ada di docker.png}
  \caption{Menambahkan route \texttt{/docker} untuk uji hot reload}
  \label{fig:p10-19-add-route-docker}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/20 - cek api docker.png}
  \caption{Hasil pengujian route \texttt{/docker}: perubahan langsung terlihat tanpa rebuild}
  \label{fig:p10-20-cek-docker}
\end{figure}

\par Dari hasil tersebut terlihat bahwa setelah file disimpan, endpoint \texttt{/docker} langsung dapat diakses dan mengembalikan response sesuai perubahan tanpa perlu rebuild image lagi, sehingga fitur hot reload dari Docker Compose berhasil diterapkan.

\subsection{Tugas \& Analisis: phpMyAdmin dan CRUD Database}
\par Pada bagian ini saya mengerjakan Tugas \& Analisis yang diberikan pada modul, yaitu menambahkan service phpMyAdmin ke dalam orkestrasi Docker Compose dan melakukan CRUD database aplikasi menggunakan phpMyAdmin.

\subsubsection{Menambahkan Service phpMyAdmin ke Docker Compose}
\par Saya menambahkan service baru bernama \texttt{phpmyadmin} menggunakan image resmi dari link yang diberikan dibawah \texttt{phpmyadmin/phpmyadmin} dari Docker Hub (\url{https://hub.docker.com/_/phpmyadmin/}). Agar tidak tabrakan dengan Apache2 di VPS (port 80), service phpMyAdmin di-forward ke port \texttt{8081} pada host (\texttt{8081:80}). Konfigurasi penambahan phpMyAdmin ditunjukkan pada Gambar \ref{fig:p10-21-add-phpmyadmin}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/21 - tambahkan phpmyadmin di docker compose.png}
  \caption{Menambahkan service phpMyAdmin pada \texttt{docker-compose.yml} dengan port \texttt{8081:80}}
  \label{fig:p10-21-add-phpmyadmin}
\end{figure}

\par Setelah konfigurasi ditambahkan, saya menjalankan Docker Compose kembali untuk membuat dan menjalankan service phpMyAdmin. Proses menjalankan Docker Compose ditunjukkan pada Gambar \ref{fig:p10-22-up-phpmyadmin}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/22 - tugas 1- jalankan docker compose up untuk membuat servis phpmyadmin.png}
  \caption{Menjalankan Docker Compose untuk membuat service phpMyAdmin}
  \label{fig:p10-22-up-phpmyadmin}
\end{figure}

\par Dengan konfigurasi tersebut, phpMyAdmin dapat diakses melalui browser pada alamat ip dengan port 8081 \texttt{http://10.33.35.96:8081}. Selain itu, konfigurasi \texttt{PMA\_HOST=database\_mysql} memastikan phpMyAdmin terhubung ke service MySQL di network Docker Compose.

\subsubsection{CRUD Database Menggunakan phpMyAdmin}
\par Setelah phpMyAdmin berjalan, saya memastikan bahwa CRUD pada database aplikasi dapat dilakukan dengan benar.

\par Pertama, saya memastikan phpMyAdmin berhasil terhubung ke database \texttt{sait\_db} dan dapat menampilkan isi tabel \texttt{users} (Gambar \ref{fig:p10-23-phpmyadmin-browse}).

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/23 - tugas 2 - tampilan php myadmin.png}
  \caption{phpMyAdmin berhasil menampilkan database \texttt{sait\_db} dan tabel \texttt{users}}
  \label{fig:p10-23-phpmyadmin-browse}
\end{figure}

\par Selanjutnya saya melakukan \textit{Create} dengan menambahkan data user baru menggunakan menu \texttt{Insert}. Form pengisian data ditunjukkan pada Gambar \ref{fig:p10-24-insert} dan hasil insert ditunjukkan pada Gambar \ref{fig:p10-25-insert-result}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/24 - tugas 2 - langkah insert.png}
  \caption{CRUD (Create): memasukkan data baru ke tabel \texttt{users}}
  \label{fig:p10-24-insert}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/25 - tugas 2- hasil insert.png}
  \caption{Hasil CRUD (Create): data baru berhasil ditambahkan ke tabel \texttt{users}}
  \label{fig:p10-25-insert-result}
\end{figure}

\par Setelah itu, saya melakukan \textit{Update} dengan mengubah nilai \texttt{last\_name} pada salah satu data user. Proses update ditunjukkan pada Gambar \ref{fig:p10-26-update}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/26 - tugas 2 - edit lastname.png}
  \caption{CRUD (Update): mengubah data \texttt{last\_name} pada tabel \texttt{users}}
  \label{fig:p10-26-update}
\end{figure}

\par Terakhir, saya melakukan \textit{Delete} untuk menghapus data yang telah ditambahkan. Proses delete ditunjukkan pada Gambar \ref{fig:p10-27-delete} dan hasil delete ditunjukkan pada Gambar \ref{fig:p10-28-delete-result}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/27 - tugas 2 - proses delete.png}
  \caption{CRUD (Delete): proses konfirmasi penghapusan data}
  \label{fig:p10-27-delete}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{resource/dokumentasi/28 - tugas 2 - hasil delete.png}
  \caption{Hasil CRUD (Delete): data berhasil dihapus dari tabel \texttt{users}}
  \label{fig:p10-28-delete-result}
\end{figure}

\par Dengan rangkaian CRUD tersebut, dapat disimpulkan bahwa service phpMyAdmin berhasil ditambahkan ke Docker Compose, terhubung dengan service MySQL, dan dapat digunakan untuk mengelola data database aplikasi.

\section{Kesimpulan}

\par Kesimpulan dari praktikum \textit{Docker Compose} adalah sebagai berikut:
\begin{enumerate}
  \item Konfigurasi koneksi database berhasil dimigrasikan dari model \textit{hardcode} ke model \texttt{.env}, sehingga parameter koneksi dapat diubah tanpa memodifikasi kode.
  \item Docker Compose berhasil digunakan untuk mengorkestrasi beberapa service dalam satu konfigurasi, yaitu service API PHP (\texttt{be-jwt}) dan service database MySQL (\texttt{database\_mysql}).
  \item Persistent volume \texttt{db\_data} berhasil diterapkan untuk menyimpan data MySQL secara persisten, serta SQL init \texttt{php\_jwt\_db.sql} membantu pembuatan database saat pertama kali container dijalankan.
  \item Konfigurasi \texttt{depends\_on} dan pengubahan \texttt{DB\_HOST} ke \texttt{database\_mysql} membantu memastikan koneksi API ke database berjalan melalui jaringan internal Docker Compose.
  \item Fitur volume binding pada service API berhasil digunakan untuk \textit{hot reload}; perubahan kode dapat langsung terlihat tanpa rebuild image.
  \item Service phpMyAdmin berhasil ditambahkan ke Docker Compose dengan port forwarding \texttt{8081:80} dan terbukti dapat melakukan operasi CRUD pada tabel \texttt{users}.
  \item Hoppscotch mempermudah pengujian endpoint karena base URL dan token JWT dapat disimpan sebagai environment variable, sehingga request dapat dijalankan berulang dengan konsisten.
\end{enumerate}

\section{Daftar Pustaka}
\printbibliography[heading=none]

\end{document}